{
  "name": "agent-swarm-visualizer",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.5.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "dev": "sh -c 'pnpm run dev:backend & pnpm run dev:dashboard & wait'",
    "demo": "sh -c 'pnpm run dev:backend & pnpm run dev:dashboard & sleep 4 && pnpm seed && wait'",
    "seed": "pnpm --filter dummy-swarm exec tsx src/index.ts --run-name \"Demo Run\" --seed 1 --speed 1 --duration 60",
    "seed:log": "pnpm --filter dummy-swarm exec tsx src/index.ts",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
