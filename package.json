{
  "name": "agentswarm",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent coding harness",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "typecheck": "turbo run typecheck",
    "poke:dev": "npx tsx poke/server.ts",
    "poke:tunnel": "npx poke tunnel http://localhost:8787/mcp --name \"AgentSwarm\"",
    "poke:ngrok": "ngrok http 8787",
    "poke:wrap": "npx poke"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "poke": "^0.2.5",
    "zod": "^4.3.6"
  }
}
